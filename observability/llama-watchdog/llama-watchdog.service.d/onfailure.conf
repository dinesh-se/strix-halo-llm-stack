[Unit]
# Restart=always covers a crash-loop, but if systemd gives up (start limit) the
# unit lands in `failed` and nothing said so between 2026-08-13 (Grafana
# retired, taking ai-stack-watchdog-down with it) and 2026-08-25.
OnFailure=telegram-alert@%n.service
